apiVersion: v1 
kind: ConfigMap 
metadata: 
  name: cart-config

data: 
  config.yaml: |  
    env: local

    server:
      address: ":8082"

    vehicle: 
      url: http://vehicle-service:8081

    db:
      host: postgres-service
      port: 5432
      user: postgres
      dbname: vehicle_management

    redis:
      host: redis-service
      port: 6379
      db: 0

    jwt:
