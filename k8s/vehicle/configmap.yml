apiVersion: v1 
kind: ConfigMap 
metadata: 
  name: vehicle-config

data:
  config.yaml: |
    env: local

    server:
      address: ":8081"

    db:
      host: postgres-service
      port: 5432
      user: postgres
      dbname: vehicle_management

    redis: 
      host: redis-service
      port: 6379
      db: 0

    jwt: